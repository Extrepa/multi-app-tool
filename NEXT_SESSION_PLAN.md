## Next Session Plan

- **Vibe UI polish**
  - Make wave previews type-aware; clarify flicker vs motion labels; add optional numeric inputs per stack item.
  - Improve color-cycle targeting by editing SVG fill/stroke for selected targets (minimize CSS filter fallback).

- **Rig presets**
  - Add a second character preset (Errl “friend”) and palette swap; ensure spawn/export flows cover new rigs.

- **Export UX**
  - Add an “Export Errl (HTML/JS)” button in the UI that copies the snippet with a confirmation toast.

- **Stability & seeding**
  - Harden duplicate checks when seeding/spawning; allow inserting prefab into the current layer when desired.

- **Testing**
  - Quick sanity checks for seeded assets/components and spawned prefab placement; verify build stays green.
